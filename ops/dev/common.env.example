# Common dev harness configuration (local only).
# Copy to ops/dev/common.env and edit as needed.

# Command to start backend service.
# Example: DEV_BACKEND_CMD="python -m autocapture_nx run"
# DEV_BACKEND_CMD=

# Optional: command to start UI (if a UI exists in this repo).
# DEV_UI_CMD=

# Optional: base URL that the UI should use to reach the backend.
# DEV_UI_BACKEND_URL=
