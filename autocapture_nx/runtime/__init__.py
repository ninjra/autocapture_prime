"""NX runtime adapters.

The repository contains a mature runtime implementation under `autocapture.runtime`
used by the kernel. The `autocapture_nx.runtime` package provides stable
import paths for traceability and future NX-first refactors.
"""

